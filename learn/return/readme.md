# Return

`return` gives a value back to whatever called the function, and the type in front of the function name is what you have to give back.

Also finally understood why `main` is `int`, it returns an error code to the OS, `0` = fine, anything else = something broke.
