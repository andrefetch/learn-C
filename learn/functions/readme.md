# Functions in C!

Honestly, love functions as an idea (from making a lot of tasks in python easier with functions (DRY!) ).

```void happyBirthday(char name[], int age) {

  return 0;
  
}```

when it takes params, you also have to infer the type.
