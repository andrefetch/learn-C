# User Input

### Basics
Completely different than how python interpets input, its not just `name = input()`, it's abstracted a lot in python where in C we have a scan function that uses a
format specifier (for non-whitespace input)
