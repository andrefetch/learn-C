#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main() {

    // math functions !

    int x = 45;

    // x = sqrt(x); sqrt
    // x = pow(x, 3); pow func
    // x = round(x);
    // x = ceil(x);
    // x = floor(x);
    // x = abs(x);
    // x = log(x);
    // x = sin(x);
    // x = cos(x);
    // x = tan(x);

    printf("%d\n", x);

    return 0;

}
