# Variable Scopes

Where a variable can be seen from, looked up in LEGB order (local, enclosed, global, built-in) so a local `result` shadows the global one. Globals are bad practice, anything in the file can change them.
